import { CMPage } from '@/components/layouts/types'

interface CollectionProps {}

const Collection: CMPage = (props: CollectionProps) => {
  return <h1>collection</h1>
}

export default Collection

Collection.Layout = 'Main'
